# uaal-playground

Unity-as-a-Library Swift Example

1. Build the Unity project with iOS settings.
2. Build the exported Xcode project using framework target.
3. Move UnityFramework.framework to uaal/uaal/.
4. Build the Xcode project, run the app on your device.
